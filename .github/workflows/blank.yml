name: TESTE PASSWORD
on: 
  workflow_dispatch:
    inputs:
      password:
        description: password     
        required: true
jobs:
  build:
    if: ${{ secrets.ref == 'refs/heads/main' }}
    runs-on: ubuntu-latest
    
    strategy:
      matrix:
        node-version: [12.13.0]
        
    steps:
    - run: |
        echo "Log level: ${{ github.event.inputs.password }}"
    - uses: actions/checkout@v1
    - name: my-step
      run: echo "Hello World!"
