name: TESTE PASSWORD
on: 
  workflow_dispatch:
    inputs:
      password:
        description: password     
        required: true
jobs:
  build:
    runs-on: ubuntu-latest
    env:
       INPUTS_PASSWORD: ${{ secrets.INPUTS_PASSWORD }}

    steps:
    - if: ${{ env.INPUTS_PASSWORD == 'true' }}
      run: echo "Hello World!"
